export type TaskStorage = { taskID: number; task: string; isDone: boolean }[];
export type TaskItem = { taskID: number; task: string; isDone: boolean };
export interface InputType {
  value: string;
}
